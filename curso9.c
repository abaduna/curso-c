#include <stdio.h>

typedef int (*compere_fun)(int a, int b);


int compere (int a, int b, compere_algo);

int main(int argc, char const *argv[]) {
   // print"f() displays the string inside quotation
    printf("%d %d\n",a ,b);
    int result = compa
   return 0;
}